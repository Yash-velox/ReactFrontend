# Template → copy to .env.uat (build-time; ReactFrontend is bundled into ShopifyApp)
APP_TYPE=uat
# Same-origin path served by Shopify Node /backend-api proxy (or nginx rewrite)
VITE_API_BASE_URL=/backend-api
VITE_SHOPIFY_API_KEY=
