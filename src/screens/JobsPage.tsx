/** Jobs page — same structure as before; Polaris look only. */
export default function JobsPage() {
  return (
    <s-page heading="Jobs">
      <s-section heading="Processing jobs">
        <s-paragraph>Batch AI processing jobs and queue status.</s-paragraph>
      </s-section>
      <s-section heading="Coming soon">
        <s-paragraph>Create jobs, track batches of up to 250 images.</s-paragraph>
      </s-section>
    </s-page>
  );
}
